<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:padding="20dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/txtTitulo"
        android:text="Selecciona tu mecánico"
        android:textStyle="bold"
        android:textSize="20sp"
        android:textColor="@color/mecha_gray_dark"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <EditText
        android:id="@+id/edtIdMecanico"
        android:layout_width="0dp"
        android:layout_height="50dp"
        android:hint="ID del mecánico (opcional)"
        android:background="@drawable/input_mecha"
        android:padding="12dp"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/txtTitulo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <Button
        android:id="@+id/btnGuardarId"
        android:layout_width="0dp"
        android:layout_height="50dp"
        android:text="GUARDAR ID DEL MECÁNICO"
        android:backgroundTint="@color/mecha_primary"
        android:textColor="@color/mecha_white"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/edtIdMecanico"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvMecanicos"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"
        app:layout_constraintTop_toBottomOf="@id/btnGuardarId"
        app:layout_constraintBottom_toTopOf="@id/btnReportar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />



</androidx.constraintlayout.widget.ConstraintLayout>